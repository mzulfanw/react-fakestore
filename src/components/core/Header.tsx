import { Stack } from '@mantine/core'
import React from 'react'

function Header() {
  return (
    <Stack
      sx={{
        overflow: 'hidden',
        backgroundColor: '#333',
        position: 'fixed',
        bottom: '50%',
        width: '100%',
        maxWidth: 'in'
      }}
    >
      test
    </Stack>
  )
}

export default Header